{
  "name": "gerencia-servicos-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd backend && npm install && cd .. && cd frontend && npm install && npm run build"
  }
}